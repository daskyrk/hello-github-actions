{
  "name": "post-to-juejin",
  "version": "1.0.0",
  "description": "post markdown content in pull request to juejin",
  "main": "src/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "esbuild src/index.js --bundle --minify --platform=node --outdir=dist --target=node16 --tsconfig=tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "post",
    "juejin",
    "sync"
  ],
  "author": "daskyrk",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@actions/github": "^5.0.0",
    "axios": "^0.26.1",
    "cross-spawn": "^7.0.3",
    "puppeteer": "^13.5.1"
  },
  "devDependencies": {
    "esbuild": "^0.14.34"
  }
}
